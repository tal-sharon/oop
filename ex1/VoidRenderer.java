/**
 * A lazy Renderer which does nothing.
 */
public class VoidRenderer implements Renderer {
    /**
     * Renders nothing.
     * @param board: Board - The rendered board.
     */
    @Override
    public void renderBoard(Board board) {}
}
